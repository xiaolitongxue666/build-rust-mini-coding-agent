---
description: 每课代码对课程页面写详细注释；单课 example + 总体 main，实现只写一份
alwaysApply: true
---

# 课对照注释 · 双入口落位

实现第 N 课时，以 [byoharness](https://www.byoharness.dev/index.html) 该章和 `follow_along/zh/NN-*.md` 为准。已完成课并进 `src/` 库；不要复制第三份 agent。

## 注释对课程页面

- 单课 example 文件头写该章标题、原章链接、本课多了哪一步。
- 库模块关键处用中文写「为什么」，标课号。课上的图、词表、步骤、陷阱要能对上。
- 不要删已有课号注释。不要提前写后续课的抽象。

## 结构和放置位置

| 放哪 | 做什么 |
|---|---|
| `examples/NN-<slug>.rs` | 本课小 demo：薄入口 + 课程页注释；`[[example]] lesson_NN`，`test = true` |
| `src/lib.rs` 各模块 | 共享实现（循环、工具、门、`Provider`、UI）只此一份 |
| `src/main.rs` | 总体：接线全部已完成课，对齐官网 `main.go` |
| `follow_along/zh/NN-*.md` | 对课正文 |
| `scripts/lib/lessons.sh` | `ready` + `cargo_example:lesson_NN` |
| `scripts/run.sh` | 无参总体；`run.sh 01` 单课 |
| `scripts/test.sh` | 总体 `cargo test` |
| `scripts/test-lessons.sh` | 单课 example 测试 |
| `scripts/test-flow.sh` | 默认离线 check + 键位清单；`--live` 管道测门和 `.local/live-probe` |

后续课同一模式：先 example，再并进库与 `main.rs`。
